"""
Custom agent tracing example — framework-agnostic manual spans.

Demonstrates using tracer.span() with any Python code, no framework required.

Run:
    python examples/custom_agent.py
"""

# Phase 2 — full implementation once tracer.py is built
print("TraceAI custom_agent example — coming in Phase 2 (tracer implementation).")
